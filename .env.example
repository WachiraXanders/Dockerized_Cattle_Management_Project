# Used only by docker-compose when building the frontend image.
# Must be a URL reachable from the BROWSER (not from inside Docker) —
# e.g. https://api.yourfarm.com, not http://backend:8000.
VITE_API_URL=http://localhost:8000
